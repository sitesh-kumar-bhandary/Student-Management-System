spring:
  application:
    name: student-management-system

  datasource:
    url: jdbc:mysql://localhost:3306/student_ms?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: 2424Sitesh@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
  
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect